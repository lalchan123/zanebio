import React from "react";

const TestCode = () => {
  //   function findBalancePoint(centerPoint, leftWeights, rightWeights) {
  //     let balancePoint = centerPoint;

  //     while (true) {
  //       const leftSum = leftWeights.reduce((a, b) => a + b, 0);
  //       const rightSum = rightWeights.reduce((a, b) => a + b, 0);

  //       if (leftSum === rightSum) {
  //         return balancePoint;
  //       } else if (leftSum > rightSum) {
  //         balancePoint--;
  //         rightWeights.push(leftWeights.pop());
  //       } else {
  //         balancePoint++;
  //         leftWeights.push(rightWeights.pop());
  //       }
  //     }
  //   }

  //   // Example usage
  //   const centerPoint = 0;
  //   const leftWeights = [2, 3, 5];
  //   const rightWeights = [1, 4, 2];

  //   const balancePoint = findBalancePoint(centerPoint, leftWeights, rightWeights);
  //   console.log("Balance point:", balancePoint);

  return <div>TestCode</div>;
};

export default TestCode;
